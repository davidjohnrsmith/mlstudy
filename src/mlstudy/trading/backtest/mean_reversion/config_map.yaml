# Config map — maps short names to YAML config file paths.
# Paths are relative to this file's directory.
#
# Usage:
#   from .sweep_config import load_sweep_config_by_name
#   cfg = load_sweep_config_by_name("mr_grid_v1")

mr_grid_v1: "config.yaml"
mr_grid_debug: "config_debug.yaml"